/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  // StrictMode
  // 리액트 앱에 존재하는 잠재적인 문제를 검사하기 위해 개발 모드로 실행했을 때 두 번씩 실행함
  // 실습을 위해 false 설정
};

export default nextConfig;
